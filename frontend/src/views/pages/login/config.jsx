// Re-export central API config to keep backward compatibility
export { API_CONFIG, API_ENDPOINTS, ERROR_MESSAGES, getDatabaseChoice, setDatabaseChoice, getAuthHeaders } from '../../../config/api.js'
